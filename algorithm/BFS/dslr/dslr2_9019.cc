#include <cstdio>
#include <queue>

using namespace std;
int main(){

	int T;
	int m, n;
	queue<int> que;
	scanf("%d", &T);

	while(T--){
		scanf("%d %d", &m, &n);
		que.push(m);
		while(!que.empty()){
			int now=que.front();
			que.pop();
			if([now*2%10000])



		}
	}


}
