#include <stdio.h>

class Solution {
public:
	bool plane[][]
	int rectangleArea(vector<vector<int>>& rectangles) {
		for (int i=0; i<rectangles.size(); i++)
		{
			for (int j=0; j<4; j++)
			{

			}

		}
		
	}
};
