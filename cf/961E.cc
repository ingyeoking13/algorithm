#include <stdio.h>
#include <algorithm>
using namespace std;

int a[200001];
int cl[200001], cr[200001];
int main(){

	int n; scanf("%d", &n);
	for (int i=1; i<=n; i++) scanf("%d", &a[i]);

	for (int i=1; i<=n; i++) {

	}



}
