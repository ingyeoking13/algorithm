#include <stdio.h>

char s[(int)1e5+1];
int dx['Z'], dy['Z'];
int main()
{
  int sx, sy, ex, ey;
  scanf("%d%d%d%d", &sx,&sy, &ex, &ey);
  int x_diff = ex-sx, y_diff = ey- sy;

  int n;
  scanf("%d", &n);
  scanf("%s", s);




}
