#include <stdio.h>
#include <queue>

priority_queue<int> pq;
int a[(int)2e5];
char s[(int)2e5+3];

int main()
{

  int n, k;
  scanf("%d%d", &n, &k);
  for (int i=0; i<n; i++) scanf("%d", &a[i]);
  scanf("%s", s);

  char start = s[0];

  for (int i=1; i<n; i++)
  {
    if ( 


  }


}
