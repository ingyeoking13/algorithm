#include <stdio.h>
#include <map>

using namespace std;
map<int, int> p;
vector<int> a;

int main()
{
	for (int i=0; i<n; i++)
	{
		int tmp; 
		scanf("%d", &tmp);
		a.push_back(tmp);
		p[tmp]=tmp;
	}




}
