#include <bits/stdc++.h>

using namespace std;
int b[100000], c[100000];
int main(){
	int n; scanf("%d", &n);
	long long sum=0;

	for (int i=0; i<n; i++){
		scanf("%d", &a[i]);
		sum += (long long)a[i];

		if ( a[i] >=0 ) b[a[i]]++;
		else c[-a[i]]++;
	}

	sort(a, a+n);
	int l = n/2, r= n/2;
	int ans=0;

	long long tmp=0;
	while( l >= 0 && r < n ) {
		if (tmp > sum){

		}
		else if ( tmp < sum){

		}
		else {
			int 

		}


	}





}
