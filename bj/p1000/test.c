#include <stdio.h>
void heapify(){


}

void heapsort(int a[], ){

}

int main(){
	int a[100], b[100];
	int n; scanf("%d", &n);
	for (int i=0; i<n; i++) scanf("%d", &a[i]);
	for (int i=0; i<n; i++) scanf("%d", &b[i]);
	heapsort(a, n), heapsort(b, n);

}
