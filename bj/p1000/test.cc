#include <bits/stdc++.h>

using namespace std;

struct node { int type, size; };

vector<node> e;

int main()
{
	int n;
	cin >> n;

	for (int i=0; i<n-1;i++)
	{
		char s[2];
		int size, p;
		cin >> s >> size >> p;
	}

}