#include <stdio.h>


int a_w[(int)1e5], a_v[(int)1e5];
int d[(int)1e5][

int go(int idx, int len)
{
  if (idx == len ) return 0;

  if 




}

int main()
{
  int n, k;
  scanf("%d%d", &n, &k);
  for (int i=0; i<n; i++)
  {
    scanf("%d%d", &a_w[i], &a_v[i]);
  }

  printf("%d\n", go(0, n));

}
