#include <iostream>
using ll = long long;

using namespace std;

int type[(int)1e3];
int main()
{
    ios::sync_with_stdio(false);
    cin.tie(0);
    cout.tie(0);

    int n, m;
    cin >> n >> m;

    for (int i=0; i<n; i++)
    {
        char s[3];
        cin >> s;
    }
}