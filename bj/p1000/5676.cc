#include <stdio.h>

using namespace std;
int a[100001];
int d[100001];
int main(){
	while(1){
		int n, k; 
		if(scanf("%d %d", &n, &k)==-1) break;

		for (int i=1; i<=n; i++) scanf("%d", &a[i]);
		if (a[1]>0) 
		for (int i=1; i<=n; i++){


		}

		while(k--){
			char s[3]; scanf("%s", s);
			int x, y; scanf("%d %d", &x, &y);
			if (s[0] == 'C') a[x]=y;
			else {



			}
		}
	}

}
