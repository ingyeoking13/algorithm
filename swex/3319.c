#include <stdio.h>

int main(){
	int T; scanf("%d", &T);

	for (int i=1; i<=2000; i++){
		for (int j=1; j<=2000; j++){
			for (int k=1; k<=2000; k++){
			}	
		}
	}

	for (int tc=1; tc<=T; tc++){
		int n, x; long long d;
		scanf("%d %lld %d", &n, &d, &x);


	}
}
