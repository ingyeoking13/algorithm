#include <stdio.h>

int main()
{
  int T;
  scanf("%d", &T);

  for (int tt=1; tt<=T; tt++)
  {
    long long d[1<<17];
    memset(d, 0, sizeof(d));













    int ans=0;

    printf("#%d %d\n", tt, ans);
  }

}
