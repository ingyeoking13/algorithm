#include <iostream>

using namespace std;

int a[(int)2e5];
int main()
{
    ios::sync_with_stdio(false);
    cin.tie(0), cout.tie(0);

    int T;
    cin >> T;
    for (int tt= 1; tt<=T; tt++)
    {
        int n, k;
        cin>> n >> k;
        for (int i=0; i<n; i++)
        {
            cin >> a[i];
        }

        


    }
}